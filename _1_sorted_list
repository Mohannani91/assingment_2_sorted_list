sam_list=[(2,5),(1,2),(4,4),(2,3),(2,1)]
def tup(n): return n[-1]
def sorted_list(tuples):
  return sorted(tuples, key=tup)
print("sample list is:",sam_list)
print("sorted list is:",sorted_list(sam_list))